# Features

Estado: ✅ pronta · 🔧 em andamento · ⬜ planejada. Edite a linha; não adicione histórico.

| Feature | Req | Dono | Estado | Onde |
|---|---|---|---|---|
| Regras de colaboração e memory-bank | — | Lucas | ✅ | `CLAUDE.md`, `memory-bank/` |
| CI de PR: commits, memory-bank, segredos, testes por componente | — | Lucas | ✅ | `.github/workflows/ci.yml` (core: pytest; api, engine e extractor: `uv sync --frozen` + ruff + pytest, api com Postgres de serviço, extractor com poppler-utils; web: tsc + build; compose: config); higiene: mensagem e tamanho por commit (artefatos gerados não contam), memory-bank pelo conjunto do PR, segredos; testes da API criam `enter_test` se faltar |
| Parsing dos CSVs da Enter e UF por número CNJ | — | Lucas | ✅ | `src/core/core/colunas.py`, `src/core/core/cnj.py` |
| Contratos P1/P3 e stub do modelo | — | Lucas | ✅ | contratos em `src/core/core/{caso,modelo,docs}.py`; `StubModelo` (lookup no histórico) em `src/api/app/stubs.py`; seleção por env em `plugins.py`; sem P3 não há stub de extração: `carregar_extrator` devolve None e o portal mostra só flags, scores e recomendação (decisão 27) |
| Política de custo esperado e backtest | 1, 2, 5 | Lucas; P1 calibra | ✅ | núcleo em `src/core/core/politica.py` (11 testes); backtest com resultados reais em `src/api/app/services/backtest.py` (~15 ms nos 60k) |
| Auth por cookie, papéis advogado/gestor | 3 | Lucas | ✅ | `src/api/app/auth.py`, `routers/auth.py`; seed com 8 usuários (`senha123`) em `services/seed.py` |
| Jobs: seed, load-historico, ingest, seed-demo, mock-painel, reset-demo, reset | — | Lucas | ✅ | `python -m app.cli`; `seed-demo` lê `data/exemplos/sinteticos_processos.csv` (340 processos só com UF, sub-assunto, valor da causa, flags e escritório, todos pendentes; sem decisões simuladas, o painel do gestor só mostra decisões reais; decisão 28); todo processo sintético aponta para o mesmo PDF de exemplo (`data/cache/sinteticos/exemplo.pdf`, baixado sob demanda no único ponto gravável do container; sem rede ou sem escrita o job avisa e segue) via `services/seed_demo.popular_documentos_sinteticos`, para a tela de documentos não ficar vazia na demo. `mock-painel` é a exceção de desenvolvimento: decisões sorteadas para conferir o painel cheio, só em banco local, desfeitas por `reset-demo` (decisão 44) |
| Lista e detalhe de processos, arquivos | 3 | Lucas | ✅ | `routers/processos.py`, `routers/files.py`; `pages/advogado/{casos,caso}/` (lista com filtro todos/pendentes/decididos, busca por número, medidor de 6 subsídios e linha clicável; PDFs em nova aba, abertos ficam marcados, cada um com o comentário da IA; a lista carrega assim que a fase 1 de `POST /api/preparacao` responde (~70 ms) e a leitura dos PDFs continua em background: `PreparandoCasos` é uma faixa acima da tabela, a linha do caso ainda não lido diz "lendo documentos" e a tela do caso troca só a recomendação e o resumo por `PreparandoCaso`, mantendo cabeçalho, documentos e subsídios utilizáveis (decisão 47); os três casos vêm de `data/exemplos/<numero>/`, versionados, e os sintéticos saíram do `reset`) |
| Recomendação gravada sob a política ativa | 1, 2, 4 | Lucas | ✅ | `GET /processos/{id}/recomendacao` → `services/recomendacao.py` (get_or_create com ON CONFLICT); três saídas — acordo, defesa e instruir (subsídios a pedir antes de acordar, pelo EVSI do engine via `Scores`) |
| Decisão com aderência, justificativa e aprovação | 4 | Lucas | ✅ | `POST /processos/{id}/decisoes`; regras em `services/recomendacao.avaliar_decisao`; minutas e contato adverso só com P3 plugado |
| Resultado da negociação | 5 | Lucas | ✅ | `POST /decisoes/{id}/resultado` (colunas em `decisoes`) |
| Eventos de auditoria | 4 | Lucas | ✅ | `POST /eventos` + eventos automáticos abriu_caso, viu_recomendacao, abriu_documento |
| Dashboard de aderência + parecer IA | 4 | Lucas | ✅ | `GET /dashboard/aderencia` (por escritório, advogado, semana e desvios); `POST /dashboard/desvios/{id}/parecer` gera uma análise consultiva OpenAI sob demanda e persiste por decisão |
| Dashboard de efetividade | 5 | Lucas | ✅ | `GET /dashboard/efetividade` separa aceite/cobertura/economia operacional do potencial versionado do engine (`docs/backtest/resumo.json`) |
| Políticas: simular, publicar, versões | 1, 5 | Lucas | ✅ | `routers/politicas.py`, `services/backtest.py`; `ativar` grava `resumo_backtest`; `/api/internal/reload-historico` |
| Link mágico `/demo` para a banca | 3 | Lucas | ✅ | `GET /api/demo?t=` reserva caso livre da Banca Demo por 15 min, rate limit 20/min |
| Front: sessão automática, casos, painel executivo, política, aprovações, tema Enter | 3, 4, 5 | Lucas | 🔧 | `pages/gestor/Painel.tsx`: resumo executivo animado com impacto financeiro, acordo e aderência; pulso operacional (aderentes × desvios), fila com parecer IA e potencial do engine separado da operação; Política/Aprovações por contexto |
| Compose, Caddy, deploy VPS, standby homelab, backup | — | Lucas | 🔧 | `make up` + `make jobs-docker` testados localmente via Caddy em :8080; `make deploy`/`make backup` escritos (VPS_HOST/VPS_DIR no .env) mas VPS e standby ainda não subiram |
| Clone limpo sobe sem dados da Enter: modelo exportado, resumo do backtest, processos sintéticos nossos | — | Lucas; P1 | ✅ | modelos em `models/*.json`, `docs/backtest/resumo.json`, `data/exemplos/sinteticos.csv` (engine) e `sinteticos_processos.csv` (portal) |
| Modelo de perda v2 (logística única em 4 docs + sub + UF; dossiê/laudo com coef 0; UF encolhida para a média; IC de Laplace; acordos fora do treino) e razão de condenação | 1, 2 | P1 | ✅ | `src/enteros/policy/{model,ratio,custos}.py` → `models/*.json`; AUC 0,923 · log-loss 0,308 · ECE 0,003 OOF; métrica `custo_decisao_oof` em R$ (cenário `comparacao` do `policy.yaml`); tabela de segmentos = p previsto por célula. `make compare-models` → `docs/modelo/comparacao.{md,json}` (variantes da logística, HGB, tabela, engine v1 pelo custo OOF). Scores para o portal via adapter; `make scored` gera `data/derived/historico_scored.csv` (OOF, colunas de `COLUNAS_HISTORICO_SCORED`, não versionado) que o `load-historico` da API prefere |
| Engine v2: três ações por valor esperado (defesa, acordo, instruir), breakeven p* por caso, EVSI por conjunto de subsídios ausentes com probabilidade total, saldo devedor nos dois ramos, IC epistêmico e `decisao_sensivel`; escada de negociação; backtest do engine | 1, 2, 5 | P1 | ✅ | `src/enteros/policy/{engine,negotiation,custos}.py`, `policy.yaml` v2 (cenário `jec`, `evsi_min_pct_causa`, `q_doc_padrao`), `src/enteros/backtest/` (replay com p OOF, baselines, banda por fold/bootstrap, breakeven, sensibilidade a aceite/s50/custos/JEC, instruir, por UF), `docs/backtest/`, `make backtest`; `make analises` → `docs/analises/` (severidade por UF, fronteira de política retorno × robustez ao aceite, curva de aceite bayesiana censurada + simulador, figuras da demo; só leitura, `src/enteros/analise/`); campos aditivos em `Recomendacao` (`p_breakeven`, `instrucao`, `analise_subsidios`, `docs_que_fortalecem`, …) |
| API própria do engine (`POST /recomendacao`) | 3 | P1 | ✅ | `src/enteros/api/main.py`, `make engine-api` (:8001) |
| Integração engine → portal (adapter `ModeloScores`) | 1, 2 | Lucas | ✅ | `src/api/app/modelo_enteros.py` (padrão de `MODEL_IMPL`); `load-historico` pontua os 60k com a logística do engine quando não há `historico_scored.csv`; 4 testes |
| Extração LLM dos PDFs + sinais de alerta | 1, 3 | P3; base Lucas | ✅ | `src/extractor`: PDFs → segurança → brief (≈1/3 do texto) → OpenAI structured output (gpt-5-mini) devolve só `resumo` (≤ 5 bullets com fonte) e `contradicoes` (decisão 49); `DadosExtraidos` (autor, advogado, contrato, sinais, confiança) sai das regras (decisão 42); validado nos dois processos exemplo; cache em `data/cache/extractor`; `make extrair PASTA=… ARGS="--sem-llm --brief"` mostra o que iria ao LLM sem gastar; `make exemplos-docs` copia os PDFs de `docs/Caso_*/` para `data/exemplos/`; `make bench-extractor` mede a compressão de tokens (números e achados em `dados.md`, relatório em `docs/extractor/benchmark.md`); benchmark com chamadas reais (4 modelos × 2 casos × 3 chamadas, gabarito objetivo, tabelas do deck) em `docs/extractor/benchmark-llm.md`; prompt 2026-09-13.1 (bullets de até 25 palavras, contradições só com prova objetiva, decisão 43); comentário por documento no card de documentos = bullet que cita aquele documento (alta se entra numa contradição); documento com sinal `DOCUMENTO_SUSPEITO` (`fonte` = arquivo) exibe triângulo de alerta vermelho com tooltip avisando prompt injection já removido na extração; bullets e contradições que citam subsídio ausente saem; gerador `make exemplo-injecao` recria o caso local `processo_prompt-injection` (petição do processo_01 com prompt injection oculta em PDF: instrução invisível PT, texto branco 1 pt EN, blob base64) e o teste de regressão cobre as três saindo do brief e virando DOCUMENTO_SUSPEITO nos dois leitores; `make exemplos-gerados` (`extractor/gerador.py`) escreve por ficha os três casos gerados de `data/exemplos/` — injeção na petição, injeção no laudo do banco e perícia divergente (decisão 50), rotulados **autos gerados** na lista; 60 testes com dublê do LLM + 2 opt-in com a OpenAI real (`make test-llm` = `pytest --llm` sobre `docs/Caso_*/`: imprime a saída crua, tokens, latência e tamanhos; JSON completo em `data/cache/testes-llm/<rodada>/`) |
| Análise e minutas em linguagem jurídica | 3 | P3; base Lucas | 🔧 | análise montada por regra sobre a saída do LLM: `contradicoes` (LLM), pontos fracos = subsídios não entregues + sinais de severidade alta, riscos = sinais; pontos fortes, `tese_provavel_autor` e `texto` ficam vazios (decisão 49); o card do advogado (`CardAnalise.tsx`) mostra só o resumo em bullets e, se houver, as contradições, também em bullets, sob rótulo amarelo (`laranja.8`); minutas (proposta, roteiro de defesa, mensagem ao adverso) por template em `extractor/minutas.py` com os valores da recomendação; falta: minutas via LLM e campo próprio para contradições no contrato |
| Portal do advogado polido + vídeo | 3 | P4 | 🔧 | `pages/advogado/{casos,caso}/` (page, hooks, components e consts separados por tela); card de recomendação em cartão escuro (`tinta.6`, mesmo estilo do resumo executivo do gestor): círculos âmbar decorativos, tipo em serifada grande (laranja quando acordo ou instruir), anel de êxito na defesa (`AnelProbabilidade.tsx`, no lugar da barra linear), custos e economia em três tiles translúcidos, banda de oferta e avisos em âmbar sobre o escuro, sem subtítulo/fatores/nome do modelo; subsídios movidos para o card de documentos, campo de valor com decimais e justificativa opcional; card de recomendação com o terceiro estado (solicitar documentos); cabeçalho do caso reduzido a uma linha de navegação (voltar + número + UF), sem box, sem botão de copiar resumo e sem autor/sinais; sem cronômetro visível no formulário de decisão (o tempo continua indo em `tempo_analise_s`); escritório fora da tela do advogado (segue só nas visões do gestor); hierarquia visual com títulos em 600 e valores na serifada; falta `docs/video.*` |
| Gráficos do painel e tela de política | 4, 5 | Lucas | 🔧 | `@mantine/charts` no Painel (tendência de aderência e rosca de resultados); o card Potencial trocou o gráfico de barras por um comparativo de cenários (barra na escala de "defender tudo", custo, variação vs. base, linha da política destacada) mais economia potencial e composição acordo/defesa; Política ainda é a simulação versionada anterior |
| Slides, README e SETUP finais | — | P5 | ⬜ | `docs/`, `SETUP.md`, `README.md` |
